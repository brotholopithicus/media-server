extends layout

block stylesheets
  link(rel='stylesheet', href='/css/app.bundle.css')
block content
  table
    thead
      tr
        th #
        th name
        th mimetype
        th size
        th watch
        th copy link
    tbody.videoList

  script(src='/js/app.bundle.js')
